{% extends 'base.html.twig' %}

{% block body %}
   

    <div class="row">
            <div class="col-sm-12">
                <div class="card-box table-responsive">
                        <h4 class="m-t-0 header-title"><b>Type  contrats</b></h4>
                             <table id="datatable" class="table table-striped table-bordered">
                                                <thead>
                                                    <tr>
                                                        <th>Id</th>
                                                        <th>Contrat</th>
                                                        <th>Actions</th>
                                                    </tr>
                                                </thead>
        <tbody>
        {% for typecontrat in typecontrats %}
            <tr>
                <td><a href="{{ path('contra_show', { 'id': typecontrat.id }) }}">{{ typecontrat.id }}</a></td>
                <td>{{ typecontrat.contrat }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('contra_show', { 'id': typecontrat.id }) }}">voir</a>
                        </li>
                        <li>
                            <a href="{{ path('contra_edit', { 'id': typecontrat.id }) }}">modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
</div>
</div>



</div>

    <ul>
        <li>
            <a href="{{ path('contra_new') }}">nouveau type</a>
        </li>
    </ul>
{% endblock %}

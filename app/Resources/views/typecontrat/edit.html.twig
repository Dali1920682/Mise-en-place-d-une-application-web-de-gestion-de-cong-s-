{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typecontrat edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input width=100% type="submit" value="modifier" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('contra_index') }}">Retour à la liste</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Suprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

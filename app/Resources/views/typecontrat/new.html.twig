{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typecontrat creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input  type="submit" value="Créer" width: 100% />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('contra_index') }}">retour à la liste</a>
        </li>
    </ul>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typecontrat</h1>

    <table id="datatable" class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typecontrat.id }}</td>
            </tr>
            <tr>
                <th>Contrat</th>
                <td>{{ typecontrat.contrat }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('contra_index') }}">retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('contra_edit', { 'id': typecontrat.id }) }}">modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="suprimier">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

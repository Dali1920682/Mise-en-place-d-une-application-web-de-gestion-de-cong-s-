{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeprofession creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('profession_index') }}">retour à la liste</a>
        </li>
    </ul>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeprofession</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeprofession.id }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ typeprofession.profession }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('profession_index') }}">retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('profession_edit', { 'id': typeprofession.id }) }}">modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="suprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
